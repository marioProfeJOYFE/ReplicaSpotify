<Application x:Class="ReplicaSpotify.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ReplicaSpotify"
             xmlns:vm="clr-namespace:ReplicaSpotify.MVVM.ViewModel"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Aplicar ViewModel a nivel GLOBAL de la aplicación -->
        <vm:AppViewModel x:Key="AppViewModel"/>
        <!--  FIN -->
        <Color x:Key="ColorBackground">#FF121212</Color>
        <Color x:Key="ColorPanel">#FF181818</Color>
        <Color x:Key="ColorAccent">#FF1DB954</Color>
        <Color x:Key="ColorAccentHover">#FF1ED760</Color>
        <Color x:Key="ColorTextPrimary">#FFFFFF</Color>
        <Color x:Key="ColorTextSecondary">#B3B3B3</Color>
        <Color x:Key="ColorSideBackground">#212121</Color>

        <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColorBackground}" />
        <SolidColorBrush x:Key="BrushPanel" Color="{StaticResource ColorPanel}" />
        <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}" />
        <SolidColorBrush x:Key="BrushAccentHover" Color="{StaticResource ColorAccentHover}" />
        <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource ColorTextPrimary}" />
        <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource ColorTextSecondary}"/>
        <SolidColorBrush x:Key="BrushSideBackground" Color="{StaticResource ColorSideBackground}"/>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource BrushBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <Style TargetType="Grid">
            <Setter Property="Background" Value="{StaticResource BrushBackground}"/>
        </Style>

        <Style TargetType="StackPanel">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        
        

    </Application.Resources>
</Application>
